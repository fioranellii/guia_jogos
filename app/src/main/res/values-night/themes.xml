<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.GuiaPocket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/spotifyGreen</item>
        <item name="colorPrimaryVariant">@color/spotifyGreenDark</item>
        <item name="colorSecondary">@color/spotifyGreen</item>

        <!-- Fundo e texto -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="android:textColor">@color/textPrimary</item>

        <!-- Status bar / navegação -->
        <item name="android:statusBarColor">@color/spotifyBlack</item>
        <item name="android:navigationBarColor">@color/spotifyBlack</item>
    </style>
</resources>
